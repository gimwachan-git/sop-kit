# Decisions

Append-only. Never edit an accepted ADR — add a new one that supersedes it and
flip the old status line.

- [0001 — Bind docs to code with in-slice annotations, not a registry file](0001-slice-annotations-over-a-registry.md) — Accepted, 2026-07-26. Why `@domain`/`@serves` live in `index.ts`, why the check is gate-enforced, and the FSD contradiction the contract has to absorb.
